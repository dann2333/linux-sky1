From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: dann2333 <dann2333@users.noreply.github.com>
Date: Tue, 08 Apr 2026 00:00:00 +0000
Subject: [PATCH] fix: resolve CoreSight and display driver boot conflicts

On CIX Sky1 (Orion O6), enabling CoreSight ETE tracing
(CONFIG_CORESIGHT_SOURCE_ETM4X) selects PID_IN_CONTEXTIDR via Kconfig,
which adds an ISB (pipeline flush) to every context switch in the vmlinux
binary. This changes boot timing enough to expose pre-existing races in
multiple display drivers (trilin DP bus timeout 0x51, orion VOP deadlock),
because these drivers rely on implicit boot timing for power sequencing.

Fix with two complementary approaches:

1. Make PID_IN_CONTEXTIDR a runtime-toggled feature using an atomic
   variable (contextidr_tracing_active). The write_sysreg+ISB in
   contextidr_thread_switch() is skipped unless a CoreSight ETM trace
   session is actively running. The ETM4x driver increments the counter
   in etm4_enable() and decrements in etm4_disable(). This eliminates
   the context-switch overhead during boot while preserving full
   PID-in-CONTEXTIDR functionality during tracing.

2. Enable the DP driver's runtime PM infrastructure that was already
   present but commented out, so that DP hardware access is properly
   power-sequenced regardless of boot timing:
   a. Uncomment pm_runtime_enable/get_sync in trilin_dp_core_power_init()
      and pm_runtime_put_sync/disable in trilin_dp_core_power_deinit().
   b. Enable device_link from DP (consumer) to DPU (supplier) in
      trilin_dptx_cix_bind() so pm_runtime_get_sync() propagates to the
      DPU, ensuring SKY1_PD_DPU2 is active before any DP register access.
   c. Add runtime PM callbacks and SET_RUNTIME_PM_OPS.

No CoreSight functionality is affected - PID_IN_CONTEXTIDR, context ID
tracing, and all ETE/TRBE features remain fully enabled during tracing.

---
 src/arch/arm64/include/asm/mmu_context.h                  | 8 ++++++++
 src/arch/arm64/kernel/process.c                           | 12 ++++++++++++
 src/drivers/gpu/drm/cix/dptx/trilin_dptx.c               |  8 ++++----
 src/drivers/gpu/drm/cix/dptx/trilin_dptx_cix.c           | 16 +++++++++++++---
 src/drivers/hwtracing/coresight/coresight-etm4x-core.c   |  8 +++++++-
 5 files changed, 44 insertions(+), 8 deletions(-)

diff --git a/src/arch/arm64/include/asm/mmu_context.h b/src/arch/arm64/include/asm/mmu_context.h
index 000000000001..000000000002 100644
--- a/src/arch/arm64/include/asm/mmu_context.h
+++ b/src/arch/arm64/include/asm/mmu_context.h
@@ -15,6 +15,7 @@
 #include <linux/sched/hotplug.h>
 #include <linux/mm_types.h>
 #include <linux/pgtable.h>
+#include <linux/atomic.h>

 #include <asm/cacheflush.h>
 #include <asm/cpufeature.h>
@@ -27,11 +28,16 @@

 extern bool rodata_full;

+extern atomic_t contextidr_tracing_active;
+
 static inline void contextidr_thread_switch(struct task_struct *next)
 {
 	if (!IS_ENABLED(CONFIG_PID_IN_CONTEXTIDR))
 		return;

+	if (!atomic_read(&contextidr_tracing_active))
+		return;
+
 	write_sysreg(task_pid_nr(next), contextidr_el1);
 	isb();
 }
diff --git a/src/arch/arm64/kernel/process.c b/src/arch/arm64/kernel/process.c
index 000000000003..000000000004 100644
--- a/src/arch/arm64/kernel/process.c
+++ b/src/arch/arm64/kernel/process.c
@@ -72,6 +72,18 @@
 void (*pm_power_off)(void);
 EXPORT_SYMBOL_GPL(pm_power_off);

+#ifdef CONFIG_PID_IN_CONTEXTIDR
+/*
+ * Controls whether contextidr_thread_switch() writes PID to CONTEXTIDR_EL1.
+ * Defaults to 0 (disabled) so that the ISB on every context switch does not
+ * run during boot.  The ETM4x driver increments this when tracing starts
+ * and decrements when tracing stops, so the overhead only exists while
+ * CoreSight ETE tracing is actively in use.
+ */
+atomic_t contextidr_tracing_active = ATOMIC_INIT(0);
+EXPORT_SYMBOL_GPL(contextidr_tracing_active);
+#endif
+
 #ifdef CONFIG_HOTPLUG_CPU
 void __noreturn arch_cpu_idle_dead(void)
 {
diff --git a/src/drivers/hwtracing/coresight/coresight-etm4x-core.c b/src/drivers/hwtracing/coresight/coresight-etm4x-core.c
index 000000000005..000000000006 100644
--- a/src/drivers/hwtracing/coresight/coresight-etm4x-core.c
+++ b/src/drivers/hwtracing/coresight/coresight-etm4x-core.c
@@ -38,6 +38,7 @@
 #include <asm/sysreg.h>
 #include <asm/local.h>
 #include <asm/virt.h>
+#include <asm/mmu_context.h>

 #include "coresight-etm4x.h"
 #include "coresight-etm-perf.h"
@@ -881,6 +882,8 @@
 	/* The tracer didn't start */
 	if (ret)
 		local_set(&drvdata->mode, CS_MODE_DISABLED);
+	else
+		atomic_inc(&contextidr_tracing_active);

 	return ret;
 }
@@ -1057,8 +1060,10 @@
 		break;
 	}

-	if (mode)
+	if (mode) {
 		local_set(&drvdata->mode, CS_MODE_DISABLED);
+		atomic_dec(&contextidr_tracing_active);
+	}
 }

 static const struct coresight_ops_source etm4_source_ops = {
diff --git a/src/drivers/gpu/drm/cix/dptx/trilin_dptx.c b/src/drivers/gpu/drm/cix/dptx/trilin_dptx.c
index 000000000007..000000000008 100644
--- a/src/drivers/gpu/drm/cix/dptx/trilin_dptx.c
+++ b/src/drivers/gpu/drm/cix/dptx/trilin_dptx.c
@@ -1949,9 +1949,9 @@
 {
 	DP_DEBUG("enter");

-	//power on DP_CORE_PM
-	//pm_runtime_enable(dp->dev);
-	//pm_runtime_get_sync(dp->dev);
+	/* power on DP_CORE_PM */
+	pm_runtime_enable(dp->dev);
+	pm_runtime_get_sync(dp->dev);

 	if (!IS_ERR(dp->dpsub->apb_clk))
 		clk_prepare_enable(dp->dpsub->apb_clk);
@@ -1966,8 +1966,8 @@
 	if (!IS_ERR(dp->dpsub->apb_clk))
 		if (__clk_is_enabled(dp->dpsub->apb_clk))
 			clk_disable_unprepare(dp->dpsub->apb_clk);
-	//pm_runtime_put_sync(dp->dev);
-	//pm_runtime_disable(dp->dev);
+	pm_runtime_put_sync(dp->dev);
+	pm_runtime_disable(dp->dev);
 	return 0;
 }

diff --git a/src/drivers/gpu/drm/cix/dptx/trilin_dptx_cix.c b/src/drivers/gpu/drm/cix/dptx/trilin_dptx_cix.c
index 000000000009..000000000010 100644
--- a/src/drivers/gpu/drm/cix/dptx/trilin_dptx_cix.c
+++ b/src/drivers/gpu/drm/cix/dptx/trilin_dptx_cix.c
@@ -173,6 +173,8 @@

 	// dpsub->link = device_link_add(drm->dev, comp, DL_FLAG_STATELESS |
 	// 				DL_FLAG_PM_RUNTIME | DL_FLAG_RPM_ACTIVE);
+	dpsub->link = device_link_add(comp, drm->dev, DL_FLAG_STATELESS |
+					DL_FLAG_PM_RUNTIME | DL_FLAG_RPM_ACTIVE);

 	return 0;
 }
@@ -378,10 +380,20 @@
 	trilin_dp_pm_complete(dp);
 }

+static int trilin_dptx_runtime_suspend(struct device *dev)
+{
+	return 0;
+}
+
+static int trilin_dptx_runtime_resume(struct device *dev)
+{
+	return 0;
+}
+
 static const struct dev_pm_ops trilin_dptx_pm_ops = {
 	SET_SYSTEM_SLEEP_PM_OPS(trilin_dptx_pm_suspend, trilin_dptx_pm_resume)
-	//SET_RUNTIME_PM_OPS(trilin_dptx_runtime_suspend,
-	//		trilin_dptx_runtime_resume, NULL)
+	SET_RUNTIME_PM_OPS(trilin_dptx_runtime_suspend,
+			trilin_dptx_runtime_resume, NULL)
 	.prepare = trilin_dptx_pm_prepare,
 	.complete = trilin_dptx_pm_complete,
 	.resume_early = trilin_dptx_pm_resume_early,
--
2.47.1
